# Sounds

The header sound effect lives here as `fahhh.mp3`.

The header speaker button (`#top-bar-sound`) plays `sounds/fahhh.mp3` on tap,
wired in `js/app.js`. To change the file name or add more sounds, update the
`new Audio('sounds/fahhh.mp3')` reference there.
